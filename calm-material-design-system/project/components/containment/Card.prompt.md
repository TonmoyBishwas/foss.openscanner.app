The grouped container: `surface-container-highest` fill, radius 16, **no shadow** — depth comes from the tonal step against the page.

```jsx
<Card>
  <h3 style={{font: "var(--type-title)", margin: 0}}>Today</h3>
  <p style={{font: "var(--type-body-md)", color: "var(--text-secondary)"}}>3 items</p>
</Card>
<Card variant="outlined">Needs extra separation</Card>
```

- `onClick` makes it interactive (hover state layer, pointer cursor).
