Settings toggle — pill track turns `primary` when on, thumb grows 16→24px. Pair with `ListTile` via its `trailing` slot.

```jsx
<Switch checked={darkMode} onChange={setDarkMode} label="Dark mode" />
```
