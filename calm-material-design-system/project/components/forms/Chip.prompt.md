Compact chip for filters and quick actions — 32px tall, radius 12, hairline outline. Selecting swaps to `secondary-container` with a leading check.

```jsx
<Chip icon="tune" onClick={openFilters}>Filters</Chip>
<Chip selected onClick={toggle}>Vegetarian</Chip>
```
